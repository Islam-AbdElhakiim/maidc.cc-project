import { AutoCloseDirective } from './auto-close.directive';

describe('AutoCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCloseDirective();
    expect(directive).toBeTruthy();
  });
});
